<h1><%= @collage.Name %></h1>

<b>Tags:</b> <%= @collage.TagList %>

<table>
  <tr>
    <th><%= sortable_header('Artist', 'a.Name') %></th>
    <th><%= sortable_header('Group Name', 'tg.Name') %></th>
  </tr>
  <% @torrents.each do |torrent| %>
    <tr>
      <td><%= link_to torrent.first, "/artists/#{torrent[1]}" %></td>
      <td><%= torrent.last %></td>
    </tr>
  <% end %>
</table>
